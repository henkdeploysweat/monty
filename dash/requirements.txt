Flask>=3.0
snowflake-connector-python>=3.7
# S3 event source (Parquet event source)
boto3>=1.34
pyarrow>=15
pandas>=2.0
# STL seasonal decomposition for the cyclical anomaly baseline (pulls scipy).
# If absent, transform.py falls back to the flat median/MAD detector.
statsmodels>=0.14
